/**
 * Check if a value exists in an array using includes() array method.
 */
const numbers = [10, 20, 30, 40, 50];

console.log('numbers array', numbers);
console.log('numbers include 50', numbers.includes(50));
console.log('numbers include 60', numbers.includes(60));
