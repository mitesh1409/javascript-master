/**
 * Create a constant to store the value of pi and log its value.
 */
const PI = Math.PI;
console.log(`Value of pi = ${PI}`);

PI = 3.14; // This will throw an error because PI is a constant and cannot be reassigned.
