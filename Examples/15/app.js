/**
 * Create a variable with a number as a string (e.g., "5000") and log its value and type.
 */
const bankBalance = "5000";
console.log(`bankBalance: ${bankBalance}`);
console.log(`typeof bankBalance: ${typeof bankBalance}`);
