/**
 * Create a variable to store age value and check whether the person is an adult or not.
 * An adult is a person whose age is greater than or equal to 18.
 */
const age = 20;
const isAdult = age >= 18;
console.log(`Age: ${age}`);
console.log(`Is adult: ${isAdult}`);
