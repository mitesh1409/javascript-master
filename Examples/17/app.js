/**
 * Create 3 variables of types string, number and boolean. Log their values and types.
 */
const superHeroName = "Spiderman";
const superHeroAge = 20;
const isRetired = false;

console.log(`superHeroName: ${superHeroName}, type: ${typeof superHeroName}`);
console.log(`superHeroAge: ${superHeroAge}, type: ${typeof superHeroAge}`);
console.log(`isRetired: ${isRetired}, type: ${typeof isRetired}`);
