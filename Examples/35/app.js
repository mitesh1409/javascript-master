/**
 * Use slice() array method to extract the first 3 elements of an array.
 */
const numbers = [10, 20, 30, 40, 50];

const firstThreeNumbers = numbers.slice(0, 3);

console.log('original array', numbers);
console.log('first 3 numbers', firstThreeNumbers);
