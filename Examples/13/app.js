/**
 * Declare a variable using let, reassign it to a different value, and log the variable to the console before and after the reassignment.
 */
let name = "Alice";
console.log(`Name = ${name}`);

name = "Bob";
console.log(`Name = ${name}`);
