/**
 * Calculate 45 * 2 - 10 and log the result to the console.
 */
const result = 45 * 2 - 10;

console.log(`45 * 2 - 10 = ${result} <-- Using a variable to store the result.`);
console.log(`45 * 2 - 10 = ${45 * 2 - 10} <-- Calculating directly in the template literal.`);
