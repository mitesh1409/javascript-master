/**
 * Add two new elements to the start of an array using unshift() array method.
 */
const numbers = [10, 20, 30];

console.log('numbers initially', numbers);

numbers.unshift(50);
numbers.unshift(500);

console.log('numbers after update', numbers);
