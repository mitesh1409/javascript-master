/**
 * Track the value of a variable by logging it before and after it changes.
 */
let score = 0;

console.log(`Score (initial): ${score}`);

score += 10;

console.log(`Score (after update): ${score}`);

score += 5;

console.log(`Score (after update): ${score}`);
