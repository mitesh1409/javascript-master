/**
 * Log each character of the string "JavaScript" using a for...of loop.
 */
const tools = ['Git', 'Docker', 'Kubernetes', 'Vite'];

for (const tool of tools) {
    console.log(tool);
}

const language = 'JavaScript';

for (const character of language) {
    console.log(character);
}
